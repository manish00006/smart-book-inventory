import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['*'], // Allow all origins in dev for local network testing
};

export default nextConfig;
